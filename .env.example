# python-mastery env vars

# Optional. Enables the in-lesson AI tutor at /learn/.../...
# The tutor is prompted to refuse direct answers and ask questions back.
# Without this key set, the tutor UI still renders but returns 503 on send.
OPENAI_API_KEY=
